import React from 'react';
import {Text, ScrollView} from 'react-native';

const Dashboard = () => {
  return (
    <ScrollView>
      <Text>Hello</Text>
    </ScrollView>
  );
};

export default Dashboard;
